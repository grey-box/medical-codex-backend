services:
  fastapi_backend:
    build:
      context: ./
    env_file:
      - .env
    expose:
      - "8080"
    networks:
      - codex-network
    ports:
      - 8080:8080
    develop:
      watch:
        - action: sync
          path: ./app
          target: /app
        - action: rebuild
          path: ./Dockerfile
networks:
  codex-network:
    name: medical-codex-net
